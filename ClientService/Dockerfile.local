# Dockerfile optimisé pour build local (évite les problèmes DNS)
# Utilise le JAR précompilé localement
FROM eclipse-temurin:17-jre
WORKDIR /app
COPY target/*.jar app.jar
EXPOSE 8081
ENTRYPOINT ["java", "-jar", "app.jar"]


